{
  "name": "benchmarking-databases",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "start-docker": "docker run -d --name redis-test -p 6379:6379 redis",
    "stop-docker": "docker stop redis test",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "redis": "^2.8.0"
  }
}